version: '3.8'

services:
  navipilot-web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./database.db:/app/database.db
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
